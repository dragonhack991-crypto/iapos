DATABASE_URL="postgresql://postgres:postgres@db:5432/iapos"
JWT_SECRET="cambia-esto-por-un-secreto-seguro-de-al-menos-32-caracteres"
APP_URL="http://localhost:3000"
NODE_ENV="development"
# COOKIE_SECURE: set to "true" only when serving over HTTPS.
# Leave as "false" (or unset) for HTTP LAN / development deployments.
COOKIE_SECURE="false"
